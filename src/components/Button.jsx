import React from "react";

function Button(props){
    return(
        <button type="button" className={props.buttonClass}>Start 14days of free Pro plan</button>
        
    )
}

export default Button;